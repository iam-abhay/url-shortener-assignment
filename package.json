{
  "name": "url-shortener-assignment",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix frontend install && npm --prefix frontend run build && node -e \"import('fs').then(f=>f.cpSync('frontend/dist', 'dist', {recursive: true}))\"",
    "start": "node backend/src/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^5.1.0",
    "pg": "^8.16.3"
  },
  "optionalDependencies": {
    "better-sqlite3": "^13.0.3"
  }
}
